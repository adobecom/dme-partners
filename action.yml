name: 'Nala'


runs:
  using: 'composite'
  steps:
    - name: Set up Node
      uses: actions/setup-node@v3
      with:
        node-version: 24.11.1

    - run: $GITHUB_ACTION_PATH/run.sh
      shell: bash
